
<div style="border:2px solid var(--gold); border-radius:12px; padding:1rem; text-align:center;">
  <img src="{{ product.image }}" alt="{{ product.name }}" style="width:100%; border-radius:8px;">
  <h3>{{ product.name }}</h3>
  <p>{{ product.price }}</p>
  <button class="btn">أضف للسلة</button>
</div>
